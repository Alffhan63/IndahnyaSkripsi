<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alfhanrf.indahnyaskripsi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splashcreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Spinner_fungsi" />
        <activity android:name=".Act_form_hunian" />
        <activity android:name=".Act_form_keagamaan" />
        <activity android:name=".Act_form_usaha" />
        <activity android:name=".Act_form_budaya" />
        <activity android:name=".Act_form_khusus" />
        <activity android:name=".Act_form_campuran"></activity>
    </application>

</manifest>